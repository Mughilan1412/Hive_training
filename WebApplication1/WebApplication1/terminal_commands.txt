git bash
----------------------------------------
docker build -t dotnet-env:1 .
-------------------------------------------
docker run -it -d -p 5000:80 \
  -e DOTNET_URLS="http://+:80" \
  -e ASPNETCORE_ENVIRONMENT=Docker \
  -e InputEnvironmentName=Docker \
  -e Database__ConnectionString="Docker_connection" \
  dotnet-env:2
----------------------------------------------