apiVersion: v1
kind: Pod
metadata:
  name: {{ include "dotnet-sample-app.fullname" . }}-test-connection
  labels:
    app: {{ include "dotnet-sample-app.fullname" . }}-test
spec:
  containers:
    - name: curl
      image: curlimages/curl:7.85.0
      command: ["sleep", "3600"]
